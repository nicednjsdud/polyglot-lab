nmae:nginx
image: nginx:1.24
port: 80

metadata:
  name: my-pod
  labels:
    app: nginx

fruits:
  - apple
  - banana
  - cherry

containers:
  - name: nginx
    image: nginx:1.24
    ports:
      - containerPort: 80

tag: "1.24"
port: 80
path: "/api/v1"

apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
    - name: nginx
      images: nginx:1.4

